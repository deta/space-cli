{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to dlv",
            "type": "go",
            "debugAdapter": "dlv-dap",
            "request": "attach",
            "mode": "remote",
            "remotePath": "${workspaceFolder}",
            "port": 2345,
            "host": "127.0.0.1",
            "preLaunchTask": "Launch Debugger (Headless)"
        },
    ],
}
